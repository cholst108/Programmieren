#include <stdio.h>

int main(void){

  // Varialbe count
	int count = 0;

  // Schleife um etwas zu sagen...
	for (count = 0; count < 100; count++){
    printf("Hallo Nr. %d", count);
  }

	return 0;
}
